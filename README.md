# Одностраничный сайт АтлантГаз

Сайт сверстан на **HTML**, **CSS**, **SCSS**, **JavaScript** и собран на **Gulp**.

## Команды для запуска и сборки проекта

- `gulp` — запуск проекта
- `gulp build` — сборка проекта
